# Feedback Processor

This project was developed with `sanity SDK` through the course of `day-one-operations > build content apps with sanity app SDK`.
<br/>
It is a bespoke application for content operations, in a nutshell a **custom app for content operations** powered by `sanity`.
